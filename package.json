{
  "name": "read-json-sync",
  "version": "1.1.0",
  "description": "Read and parse a JSON file synchronously",
  "repository": "shinnn/read-json-sync",
  "author": "Shinnosuke Watanabe (https://github.com/shinnn)",
  "scripts": {
    "pretest": "eslint --config node_modules/@shinnn/eslintrc-node/rc.json index.js test/test.js",
    "test": "node --harmony_arrow_functions test/test.js | tap-spec",
    "coverage": "node --harmony_arrow_functions node_modules/.bin/istanbul cover test/test.js",
    "coveralls": "${npm_package_scripts_coverage} && istanbul-coveralls"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "keywords": [
    "json",
    "file",
    "read",
    "parse",
    "synchronous",
    "sync"
  ],
  "dependencies": {
    "graceful-fs": "^4.1.2"
  },
  "devDependencies": {
    "@shinnn/eslintrc-node": "^1.0.2",
    "eslint": "^0.22.1",
    "istanbul": "^0.3.14",
    "istanbul-coveralls": "^1.0.2",
    "tap-spec": "^4.0.0",
    "tape": "^4.0.0"
  }
}
